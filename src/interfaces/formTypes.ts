export type LoginForm = {
  efin: string;
  email: string;
  password: string;
};

export type RegisterForm = {
  firstName: string;
  lastName: string;
  email: string;
  companyName: string;
  efin: string;
};
